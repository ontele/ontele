{{- if .Values.loki.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ontele.component" (dict "root" . "name" "loki") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ontele.componentLabels" (dict "root" . "component" "loki") | nindent 4 }}
data:
  loki.yml: |
    {{- .Files.Get "files/loki.yml" | nindent 4 }}
{{- end }}
